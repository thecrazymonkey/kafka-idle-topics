# Claude Code Rules

## GitHub Interactions

Always use the `gh` CLI for all GitHub interactions (issues, pull requests, checks, releases, comments, etc.). Do not use the GitHub web API directly or other tools when `gh` can accomplish the task.
